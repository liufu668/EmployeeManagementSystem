<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.hrms.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.study.hrms.entity.User">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="realname" jdbcType="VARCHAR" property="realname" />
        <result column="last_login_time" jdbcType="TIMESTAMP" property="lastLoginTime" />
        <result column="createtime" jdbcType="TIMESTAMP" property="createtime" />
        <result column="paword" jdbcType="VARCHAR" property="paword" />
    </resultMap>

    <sql id="Base_Column_List">
        id,
        username,
        realname,
        last_login_time,
        createtime,
        paword

    </sql>

    <insert id="insert" parameterType="com.study.hrms.entity.User">
        insert into user (
        id,
        username,
        realname,
        last_login_time,
        createtime,
        paword
        )
        values (
        #{id,jdbcType=INTEGER},
        #{username,jdbcType=VARCHAR},
        #{realname,jdbcType=VARCHAR},
        #{lastLoginTime,jdbcType=TIMESTAMP},
        #{createtime,jdbcType=TIMESTAMP},
        #{paword,jdbcType=VARCHAR}
        )
    </insert>


    <update id="updateByPrimaryKey" parameterType="com.study.hrms.entity.User">
        update user
        set

        username = #{username,jdbcType=VARCHAR},

        realname = #{realname,jdbcType=VARCHAR},

        last_login_time = #{lastLoginTime,jdbcType=TIMESTAMP},

        createtime = #{createtime,jdbcType=TIMESTAMP},

        paword = #{paword,jdbcType=VARCHAR}

        where

        id = #{id,jdbcType=INTEGER}

    </update>
    <update id="updateByPrimaryKeySelective">
        update user
        <set>
            <if test="username != null">
                username = #{username,jdbcType=VARCHAR},
            </if>
            <if test="realname != null">
                realname = #{realname,jdbcType=VARCHAR},
            </if>
            <if test="lastLoginTime != null">
                last_login_time = #{lastLoginTime,jdbcType=TIMESTAMP},
            </if>
            <if test="createtime != null">
                createtime = #{createtime,jdbcType=TIMESTAMP},
            </if>
            <if test="paword != null">
                paword = #{paword,jdbcType=VARCHAR}
            </if>
        </set>
        where
        id = #{id,jdbcType=INTEGER}

    </update>
    <delete id="deleteByPrimaryKey">
        delete
        from user
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <select id="findByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
        select * from user where username = #{username,jdbcType=VARCHAR}
    </select>
    <select id="list" parameterType="com.study.hrms.qo.UserQO" resultMap="BaseResultMap">
        select * from user
        where 1=1
            <if test="username != null">
                and username = #{username,jdbcType=VARCHAR}
            </if>
            <if test="realname != null">
                and realname = #{realname,jdbcType=VARCHAR}
            </if>
    </select>
    <select id="selectByPrimaryKey" resultType="com.study.hrms.entity.User">
        select
        <include refid="Base_Column_List"></include>
        from user
        where id = #{id,jdbcType=INTEGER}
    </select>
</mapper>

